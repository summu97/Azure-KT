* directly use azure SDK to pull secrets and pass as env variables to required file.
* add commands in the file itself, where it requires the secrets and pull and use them.
* use another script or some source that pulls the secrets and pass them as env variables to required file and when the process is done, the env variables are automatically 
  removed so that others cant get the secrets.
