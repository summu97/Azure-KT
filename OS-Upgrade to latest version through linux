Upgrading when connected to VM using SSH connect.
Pre-requesites: Continuous Network and Power
RUN:
sudo apt update
sudo apt upgrade -y
sudo do-release-upgrade -c
sudo do-release-upgrade
Go with the steps it prompt and mostly pass 'y'
